# Aboug

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 21.2.5.

## Andorid

To build Andorid ap

```bash
ng b
npx cap sync
npx cap open android
```

On opening Android Studio 
`Build` > `Generate Signed App Bundle or APK` > `Generate APKs` or `Generate Bundles`
file will be in
[/android/app/build/outputs/apk/debug](./android/app/build/outputs/apk/debug) or 