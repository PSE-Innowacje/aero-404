export interface AirfieldRequestDto {
  name: string;
  latitude: number;
  longitude: number;
}

export interface AirfieldResponseDto {
  id: number;
  name: string;
  latitude: number;
  longitude: number;
}
